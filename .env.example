NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# App origin (set in local dev, e.g. http://localhost:3000 — avoids ambiguous www vs app middleware)
NEXT_PUBLIC_APP_URL=

# Optional: for AI listing description generation (GPT-3.5)
OPENAI_API_KEY=
